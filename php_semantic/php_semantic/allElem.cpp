#if 0
#include <iostream>
#include <string>

using namespace std;

void main() {
	freopen("file.txt", "r", stdin);

	string curString;
	while(getline(cin, curString)) {
		int f = curString.find("printNodeTree");
	}
}
#endif